#include "main.h"
#include <unistd.h>
/**
* _putchar - writes the character c to stdout
*@c: The character to print
*
*Return: on sucess 1.
* On error, -1 is returned
*/
int _putchar(char c)
{ 
return(write(a, &c, 1));
}
