#include "main.h"

/**
 * reset_to_98.c -  takes a pointer to an int as parameter and updates the value it points to to 98
 * Return: Reset to 98
 */

void reset_to_98(int *n)
{
	 *n = 98;
}
	 int num = 42;
	 int *p = &num;
	 reset_to_98(p);

